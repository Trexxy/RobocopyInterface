<Window x:Class="RobocopyInterface.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="RootGrid" RequestedTheme="Light">
        <Grid.Resources>
            <LinearGradientBrush x:Key="PageBackgroundBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#FAF9FF" Offset="0"/>
                <GradientStop Color="#F3F1FE" Offset="0.5"/>
                <GradientStop Color="#FDF2F8" Offset="1"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="AccentBrush" Color="#635BFF"/>
            <SolidColorBrush x:Key="AccentTintBrush" Color="#EEECFF"/>
            <LinearGradientBrush x:Key="AccentGradientBrush" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="#635BFF" Offset="0"/>
                <GradientStop Color="#8B7CFF" Offset="1"/>
            </LinearGradientBrush>
            <SolidColorBrush x:Key="CardBorderBrush" Color="#E6E4F5"/>
            <SolidColorBrush x:Key="RowFillBrush" Color="#F8F8FC"/>
            <SolidColorBrush x:Key="MutedTextBrush" Color="#6B7280"/>

            <Style x:Key="CardStyle" TargetType="Border">
                <Setter Property="CornerRadius" Value="16"/>
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Padding" Value="20"/>
                <Setter Property="Shadow">
                    <Setter.Value>
                        <ThemeShadow/>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="SectionHeaderStyle" TargetType="TextBlock">
                <Setter Property="FontSize" Value="15"/>
                <Setter Property="FontWeight" Value="SemiBold"/>
                <Setter Property="Foreground" Value="#1F2430"/>
                <Setter Property="VerticalAlignment" Value="Center"/>
            </Style>
            <Style x:Key="MutedTextStyle" TargetType="TextBlock">
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
                <Setter Property="FontSize" Value="12"/>
            </Style>
            <Style x:Key="RowIconButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Padding" Value="0"/>
                <Setter Property="Width" Value="30"/>
                <Setter Property="Height" Value="30"/>
                <Setter Property="CornerRadius" Value="15"/>
                <Setter Property="Background" Value="Transparent"/>
                <Setter Property="BorderThickness" Value="0"/>
                <Setter Property="Foreground" Value="{StaticResource MutedTextBrush}"/>
            </Style>
            <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource DefaultButtonStyle}">
                <Setter Property="Background" Value="White"/>
                <Setter Property="BorderBrush" Value="{StaticResource CardBorderBrush}"/>
                <Setter Property="BorderThickness" Value="1"/>
                <Setter Property="Foreground" Value="#1F2430"/>
                <Setter Property="CornerRadius" Value="8"/>
            </Style>
        </Grid.Resources>

        <Grid.Background>
            <StaticResource ResourceKey="PageBackgroundBrush"/>
        </Grid.Background>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>   <!-- Custom title bar -->
            <RowDefinition Height="5*" MinHeight="280"/>  <!-- Sources card -->
            <RowDefinition Height="3*" MinHeight="160"/>  <!-- Log card -->
            <RowDefinition Height="Auto"/>   <!-- Status / actions card -->
        </Grid.RowDefinitions>
        <Grid.RowSpacing>16</Grid.RowSpacing>

        <!-- ============ Custom title bar ============ -->
        <Grid x:Name="AppTitleBar" Grid.Row="0" Height="52" Padding="20,0,0,0">
            <StackPanel Orientation="Horizontal" VerticalAlignment="Center" Spacing="10">
                <Border Width="28" Height="28" CornerRadius="8" Background="{StaticResource AccentGradientBrush}">
                    <FontIcon Glyph="&#xE895;" FontSize="14" Foreground="White" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="Robocopy Helper" FontSize="13" FontWeight="SemiBold" Foreground="#1F2430" VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>

        <!-- ============ Sources card ============ -->
        <Border Grid.Row="1" Style="{StaticResource CardStyle}" Margin="20,0,20,0" Translation="0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Header toolbar -->
                <Grid Grid.Row="0" Margin="0,0,0,14">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" Spacing="10" VerticalAlignment="Center">
                        <FontIcon Glyph="&#xE8B7;" FontSize="16" Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Text="Sources" Style="{StaticResource SectionHeaderStyle}"/>
                        <Border Background="{StaticResource AccentTintBrush}"
                                CornerRadius="10"
                                Padding="8,2"
                                VerticalAlignment="Center">
                            <TextBlock Text="{Binding Sources.Count}"
                                       FontSize="11"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource AccentBrush}"/>
                        </Border>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                        <Button Content="Clear"
                                Style="{StaticResource SecondaryButtonStyle}"
                                Command="{Binding ClearSourcesCommand}"
                                IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBoolConverter}}"
                                Padding="12,6"/>
                        <Button Command="{Binding AddFolderCommand}"
                                Style="{StaticResource SecondaryButtonStyle}"
                                IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBoolConverter}}"
                                Padding="12,6">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE710;" FontSize="13"/>
                                <TextBlock Text="Add Folder"/>
                            </StackPanel>
                        </Button>
                        <Button Command="{Binding AddFileCommand}"
                                IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBoolConverter}}"
                                Background="{StaticResource AccentBrush}"
                                Foreground="White"
                                CornerRadius="8"
                                Padding="12,6">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <FontIcon Glyph="&#xE710;" FontSize="13"/>
                                <TextBlock Text="Add File"/>
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </Grid>

                <!-- Empty state -->
                <StackPanel Grid.Row="1"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Spacing="6"
                            Visibility="{Binding Sources.Count, Converter={StaticResource CountToVisibilityConverter}}">
                    <FontIcon Glyph="&#xE8B7;" FontSize="28" Foreground="{StaticResource CardBorderBrush}" HorizontalAlignment="Center"/>
                    <TextBlock Text="No sources yet"
                               HorizontalAlignment="Center"
                               Style="{StaticResource MutedTextStyle}"/>
                    <TextBlock Text="Add a folder or file to start syncing"
                               HorizontalAlignment="Center"
                               Style="{StaticResource MutedTextStyle}"/>
                </StackPanel>

                <!-- Sources list -->
                <ListView Grid.Row="1"
                          x:Name="SourcesListView"
                          ItemsSource="{Binding Sources}"
                          SelectionMode="None"
                          IsEnabled="{Binding IsSyncing, Converter={StaticResource InverseBoolConverter}}"
                          Padding="0"
                          Visibility="{Binding Sources.Count, Converter={StaticResource CountToVisibilityConverter}, ConverterParameter=Invert}">
                    <ListView.ItemContainerStyle>
                        <Style TargetType="ListViewItem">
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                            <Setter Property="Padding" Value="0,0,0,10"/>
                            <Setter Property="MinHeight" Value="0"/>
                        </Style>
                    </ListView.ItemContainerStyle>
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Border Background="{StaticResource RowFillBrush}"
                                    BorderBrush="{StaticResource CardBorderBrush}"
                                    BorderThickness="1"
                                    CornerRadius="10"
                                    Padding="14,10">
                                <Grid ColumnSpacing="12" RowSpacing="6">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="Auto"/>
                                    </Grid.RowDefinitions>

                                    <FontIcon Grid.Row="0" Grid.Column="0"
                                              Glyph="{Binding Source, Converter={StaticResource PathTypeToGlyphConverter}}"
                                              FontSize="15"
                                              Foreground="#635BFF"
                                              VerticalAlignment="Center"/>
                                    <TextBlock Grid.Row="0" Grid.Column="1"
                                               Text="{Binding Source}"
                                               FontWeight="SemiBold"
                                               Foreground="#1F2430"
                                               VerticalAlignment="Center"
                                               TextTrimming="CharacterEllipsis"/>
                                    <Button Grid.Row="0" Grid.Column="2"
                                            Style="{StaticResource RowIconButtonStyle}"
                                            Command="{Binding DataContext.RemoveSourceCommand, ElementName=SourcesListView}"
                                            CommandParameter="{Binding}">
                                        <FontIcon Glyph="&#xE711;" FontSize="12"/>
                                    </Button>

                                    <FontIcon Grid.Row="1" Grid.Column="0"
                                              Glyph="&#xE72A;"
                                              FontSize="12"
                                              Foreground="{StaticResource MutedTextBrush}"
                                              VerticalAlignment="Center"/>
                                    <TextBox Grid.Row="1" Grid.Column="1"
                                             Text="{Binding Target, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                             PlaceholderText="Choose a target folder…"
                                             Background="White"
                                             BorderBrush="{StaticResource CardBorderBrush}"
                                             VerticalContentAlignment="Center"
                                             CornerRadius="8"
                                             Padding="8,6"/>
                                    <Button Grid.Row="1" Grid.Column="2"
                                            Content="Browse"
                                            Style="{StaticResource SecondaryButtonStyle}"
                                            Command="{Binding DataContext.BrowseTargetCommand, ElementName=SourcesListView}"
                                            CommandParameter="{Binding}"
                                            Padding="12,6"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>
        </Border>

        <!-- ============ Log card ============ -->
        <Border Grid.Row="2" Style="{StaticResource CardStyle}" Margin="20,0,20,0" Padding="0" Translation="0,0,20">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="8" Margin="20,14,20,10">
                    <TextBlock Text="&gt;_" FontFamily="Cascadia Mono, Consolas" FontWeight="Bold"
                               Foreground="{StaticResource AccentBrush}"/>
                    <TextBlock Text="Activity Log" Style="{StaticResource SectionHeaderStyle}"/>
                </StackPanel>

                <Border Grid.Row="1"
                        Background="#191A2E"
                        CornerRadius="0,0,16,16"
                        Padding="4">
                    <ScrollViewer x:Name="LogScrollViewer"
                                  VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Auto">
                        <TextBox x:Name="LogTextBox"
                                 Text="{Binding LogText, Mode=OneWay}"
                                 IsReadOnly="True"
                                 TextWrapping="NoWrap"
                                 FontFamily="Cascadia Mono, Consolas"
                                 FontSize="12"
                                 Foreground="#C9CCEF"
                                 Background="Transparent"
                                 BorderThickness="0"
                                 Padding="12,8"/>
                    </ScrollViewer>
                </Border>
            </Grid>
        </Border>

        <!-- ============ Status / actions card ============ -->
        <Border Grid.Row="3" Style="{StaticResource CardStyle}" Margin="20,0,20,20" Translation="0,0,20">
            <Grid ColumnSpacing="20">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Spacing="12">
                    <!-- Overall progress -->
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Overall" FontSize="12" FontWeight="SemiBold" Foreground="#1F2430"/>
                            <TextBlock Text="{Binding OverallSizeText}" Style="{StaticResource MutedTextStyle}"/>
                        </StackPanel>
                        <ProgressBar Grid.Column="1" Value="{Binding OverallProgress}" Minimum="0" Maximum="100" Height="8" VerticalAlignment="Center"
                                     Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding OverallProgressText}"
                                   VerticalAlignment="Center"
                                   MinWidth="90"
                                   Style="{StaticResource MutedTextStyle}"/>
                    </Grid>

                    <!-- Current file progress -->
                    <Grid ColumnSpacing="12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="90"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <StackPanel Grid.Column="0" VerticalAlignment="Center">
                            <TextBlock Text="Current file" FontSize="12" FontWeight="SemiBold" Foreground="#1F2430"/>
                            <TextBlock Text="{Binding CopySpeed}" Style="{StaticResource MutedTextStyle}"/>
                        </StackPanel>
                        <ProgressBar Grid.Column="1" Value="{Binding CurrentFileProgress}" Minimum="0" Maximum="100" Height="8" VerticalAlignment="Center"
                                     Foreground="{StaticResource AccentBrush}"/>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding CurrentFileProgress, Converter={StaticResource PercentTextConverter}}"
                                   VerticalAlignment="Center"
                                   MinWidth="90"
                                   Style="{StaticResource MutedTextStyle}"/>
                    </Grid>
                </StackPanel>

                <!-- Start Sync / Cancel -->
                <Grid Grid.Column="1">
                    <Button Command="{Binding StartSyncCommand}"
                            Background="{StaticResource AccentGradientBrush}"
                            Foreground="White"
                            CornerRadius="18"
                            Padding="22,0" MinWidth="130" Height="42" VerticalAlignment="Center"
                            Visibility="{Binding IsSyncing, Converter={StaticResource BoolToVisibilityConverter}, ConverterParameter=Invert}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE768;" FontSize="14"/>
                            <TextBlock Text="Start Sync" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                    <Button Command="{Binding CancelSyncCommand}"
                            Background="{ThemeResource SystemFillColorCriticalBackgroundBrush}"
                            Foreground="{ThemeResource SystemFillColorCriticalBrush}"
                            CornerRadius="18"
                            Padding="22,0" MinWidth="130" Height="42" VerticalAlignment="Center"
                            Visibility="{Binding IsSyncing, Converter={StaticResource BoolToVisibilityConverter}}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE71A;" FontSize="14"/>
                            <TextBlock Text="Cancel" FontWeight="SemiBold"/>
                        </StackPanel>
                    </Button>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</Window>
